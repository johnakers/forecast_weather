require 'rails_helper'

RSpec.describe Address, type: :model do
  # this is skipped for now as there is a diffrerence in tests with "perform_now" vs "perform_later"
  xdescribe "callbacks" do
  end
end
